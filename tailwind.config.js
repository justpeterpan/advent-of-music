/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    extend: {
      transitionProperty: {
        multiple: 'width, height, opacity, transform',
      },
    },
  },
  plugins: [],
}
